# Prometheus scrape config for ui-server.
# Consumed by otelcol-contrib's prometheus receiver or vmagent.
scrape_configs:
  - job_name: ui-server
    scrape_interval: 15s
    scrape_timeout: 10s
    metrics_path: /metrics
    static_configs:
      - targets:
          - "127.0.0.1:9090"  # replace with cfg.ports.metrics
        labels:
          service: ui-server
          host: "{{ inventory_hostname }}"
